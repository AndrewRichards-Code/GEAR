#include "AudioSerialiser.h"

using namespace gear;
using namespace asset;
using namespace serialiser;

Ref<Asset> AudioSerialiser::Deserialise(Asset::Handle handle, const AssetMetadata& metadata)
{
	return Ref<Asset>();
}

void AudioSerialiser::Serialise(Ref<Asset> asset, const AssetMetadata& metadata)
{
}
